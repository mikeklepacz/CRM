I'll audit the Matrix2 folder and related integration points to identify errors, orphans, and readiness issues.

Audit Findings
Critical Issues
1. Incomplete Integration - Matrix2 Not Actually Used
Looking at emailQueue.ts, the Matrix2 system is called but never actually assigns recipients:

ensureDailySlots() creates empty slots ✅
assignRecipientsToSlots() is called but has a fatal eligibility bug ❌
The SQL query fetches slots but the assignment logic is broken
2. Fatal Bug in slotAssigner.ts
slotAssigner.ts has a completely broken eligibility checker:

function isRecipientEligible(recipient: any, slotUtc: Date, settings: any): boolean {
  const local = utcToZonedTime(slotUtc, recipient.timezone);
  const parsed = parseBusinessHours(recipient.business_hours); // ❌ Missing state parameter
  const day = local.getDay();
  const daySchedule = parsed[String(day)]; // ❌ Wrong - should be parsed.schedule[day]
Problems:

parseBusinessHours() requires 2 params but only gets 1
Returns object with {schedule, is24_7, isClosed} but code treats it like array
Missing state-based timezone resolution
Window calculation is completely wrong
3. Orphaned Empty File
matrix2.ts is completely empty - this is an orphan that serves no purpose.

4. Missing Critical Database Fields
slotAssigner.ts queries fields that don't exist in the schema:

ready_for_send - not in sequence_recipients table
Queries use fields not defined in migrations
5. Enrollment Still Uses Old System
routes.ts shows enrollment still calls getNextMatrixSlot() from the OLD scheduler, not Matrix2:

const { getNextMatrixSlot } = await import('./services/matrixScheduler');
This completely bypasses the Matrix2 slot system!

Architecture Conflicts
6. Dual Scheduling Systems Running Simultaneously
Matrix2: Pre-generates slots, tries to assign recipients to them
Old Matrix Scheduler: Calculates individual send times on enrollment
Result: They fight each other, Matrix2 slots stay empty
7. Inconsistent Timezone Handling
matrixScheduler.ts uses formatInTimeZone() correctly ✅
slotAssigner.ts uses utcToZonedTime() but with broken business hours parsing ❌
Mismatch will cause scheduling conflicts
Missing Functionality
8. No Connection Between Enrollment and Matrix2
When a recipient is enrolled:

Old scheduler calculates scheduledAt
Stores it in sequence_scheduled_sends
Matrix2 never sees it because it looks for ready_for_send = TRUE
Field doesn't exist, so Matrix2 finds nothing
9. No Worker to Populate ready_for_send
There's no service that:

Monitors sequence_scheduled_sends.scheduledAt
Sets ready_for_send = TRUE when time arrives
Triggers Matrix2 assignment
Data Integrity Issues
10. Slot/Recipient Mismatch
daily_send_slots.recipient_id references sequence_recipients.id
But enrollment creates entries in sequence_scheduled_sends
No foreign key relationship between these tables
Fix Required
The Matrix2 system is not ready and has fundamental architectural problems. Here's what needs to happen:

server/services/Matrix2/slotAssigner.ts
+65
-37
Fix broken eligibility checker with correct business hours parsing and timezone handling
server/services/Matrix2/recipientDb.ts
+62
-28
Fix query to use actual database schema fields instead of non-existent ready_for_send
server/services/Matrix2/matrix2.ts
+6
-0
Remove orphaned empty file - this is not needed
Summary
Matrix2 is NOT ready for production. Critical issues:

❌ Broken eligibility checker (business hours parsing)
❌ Wrong database field queries (ready_for_send doesn't exist)
❌ Empty orphan file (matrix2.ts)
❌ Not integrated with enrollment flow
❌ Conflicts with old Matrix scheduler
Recommendation: Either fully commit to Matrix2 OR remove it and keep the old system. Right now they're fighting each other.


Apply all

Preview code changes
What files are "fighting" with Matrix2 ?

MK

mikeklepacz
Assistant
just now
Read 11 files
Looking at your Matrix2 scheduler implementation and the codebase, here are the files that are "fighting" with Matrix2:

Primary Conflicts
server/services/emailSchedulingService.ts - This file contains the deprecated scheduleRecipient() function that was replaced by Matrix2's getNextMatrixSlot(). It now throws an error to prevent usage, but it's still being called from:

server/routes.ts (lines ~2518-2529, ~2653-2664)
Potentially other enrollment flows
server/services/rescheduleCoordinator.ts - Uses inline scheduling logic instead of delegating to Matrix2. It should be calling matrixScheduler.getNextMatrixSlot() but implements its own calculation logic.

server/services/eligibilityPlanner.ts - May contain duplicate scheduling logic that conflicts with Matrix2's slot-based approach.

server/services/smartTiming.ts - The old Smart Timing system that Matrix2 replaced. If this is still being called anywhere, it creates conflicts.

server/services/queueCoordinator.ts - If this exists and is separate from the Matrix2 workflow, it may be trying to schedule emails independently.

The Core Issue
Matrix2 uses a pre-generated slot system:

slotGenerator.ts creates daily slots
slotAssigner.ts fills them with recipients
emailQueue.ts processes filled slots
But your enrollment code is still calling the old real-time scheduler (scheduleRecipient()), which tries to calculate scheduledAt at enrollment time rather than letting Matrix2 assign slots.

What Needs to Happen
At enrollment time, you should:

❌ NOT call scheduleRecipient() or matrixScheduler.getNextMatrixSlot()
✅ ONLY calculate eligibleAt (when the email becomes eligible based on delays)
✅ Let Matrix2's slotAssigner.ts assign the actual scheduledAt later
The Matrix2 flow should be:

Enrollment → Set eligibleAt → Matrix2 assigns slot → Matrix2 sends email
Currently you have:

Enrollment → Calculate scheduledAt (WRONG!) → Matrix2 tries to use it → Conflict
Would you like me to show you the specific fixes needed in server/routes.ts to stop calling the deprecated scheduler and properly integrate with Matrix2?