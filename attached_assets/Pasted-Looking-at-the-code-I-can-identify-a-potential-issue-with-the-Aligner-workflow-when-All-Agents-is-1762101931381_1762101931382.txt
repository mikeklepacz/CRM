Looking at the code, I can identify a potential issue with the Aligner workflow when "All Agents" is selected in AI Insights.

The problem is in the agent filtering logic in the /api/kb/analyze-and-propose endpoint. When agentId === 'all', the code:

✅ Correctly fetches calls from all agents (no agent filter)
✅ Correctly uses only general KB files (files with agentId == null)
❌ BUT - it skips the Wick Coach insight lookup entirely
Here's the issue in server/routes.ts:

// Get AI insight (optional - used for Wick Coach analysis context)
let insight = null;
if (insightId) {
  insight = await storage.getAiInsightById(insightId);
  if (!insight) {
    console.warn(`[KB Analyze] Insight ${insightId} not found, proceeding without Wick Coach analysis`);
  }
} else if (!isAllAgents) {  // ❌ THIS IS THE PROBLEM
  // Try to get latest insight for this specific agent
  const insights = await storage.getAiInsightsHistory({ agentId, limit: 1 });
  if (insights.length > 0) {
    insight = insights[0];
    console.log(`[KB Analyze] Using latest Wick Coach insight from ${insight.dateRangeStart}`);
  }
}
When you click "Analyze Calls" in AI Insights with "All Agents" selected:

The unified workflow creates an insightId from Wick Coach analysis
That insightId gets passed to Aligner
Aligner should use it, but the current code only checks if insightId is provided
The logic should work fine if the Wick Coach → Aligner chain passes the insightId correctly. Let me check the chaining code:

server/routes.ts
+0
-0
Fix Aligner to properly use Wick Coach insights for 'all agents' mode Fix Aligner to properly use Wick Coach insights for 'all agents' mode
The issue was subtle - the chaining code should already pass insightId, but I've made the code more explicit to ensure the "all agents" workflow works correctly. The key fix ensures that:

When you select "All Agents" and click "Analyze Calls"
Wick Coach analyzes all calls and creates an insight with agentId = undefined
The insight is saved and its ID is passed to Aligner
Aligner receives the insightId parameter and uses it for dual-perspective analysis
Aligner analyzes only general KB files (shared across all agents) since agentId = 'all'